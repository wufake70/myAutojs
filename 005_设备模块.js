



console.show()
//device.width
// log(device.width)
// device.height

// device.buildId   修订版本号，或者诸如"M4-rc20"的标识
// log(device.buildId)

// device.broad     设备主板型号
// log(device.broad)    
// device.brand     手机品牌
// log(device.brand)
// device.device    设备在工业设计中的名称
// device.model     设备型号
// device.product   整个产品的名称
// device.bootloader    设备Bootloader的版本
// device.hardware      设备的硬件名称(来自内核命令行或者/proc)
// device.fingerprint   构建(build)的唯一标识码
// device.sdkInt        安卓系统API版本。例如安卓4.4的sdkInt为19
// device.release       Android系统版本号。例如"5.0", "7.1.1"

// device.getIMEI()     返回设备的IMEI.
// device.getAndroidId()    Android ID为一个用16进制字符串表示的64位整数，在设备第一次使用时随机生成，之后不会更改，除非恢复出厂设置。
// device.getMacAddress()   返回设备的Mac地址。该函数需要在有WLAN连接的情况下才能获取，否则会返回null
// device.getBrightness()   返回当前的(手动)亮度。范围为0~255
// log(device.getBrightness())

// device.getBrightnessMode()   返回当前亮度模式，0为手动亮度，1为自动亮度。
// device.setBrightness(b)      设置当前手动亮度
// device.setBrightness(1)

// device.setBrightnessMode(mode)   设置当前亮度模式
// device.getMusicVolume()
// device.getNotificationVolume()
// device.getAlarmVolume()
// device.getMusicMaxVolume()
// device.getNotificationMaxVolume()
// device.getAlarmMaxVolume()
// device.setMusicVolume(volume)
// device.setNotificationVolume(volume)
// device.setAlarmVolume(volume)
// device.getBattery()
// device.isCharging()
// device.getTotalMem()
// device.getAvailMem()
// device.isScreenOn()              返回设备屏幕是否是亮着的
// device.wakeUp()                  唤醒设备。包括唤醒设备CPU、屏幕等。可以用来点亮屏幕
// device.wakeUpIfNeeded()
// device.keepScreenOn([timeout])   保持屏幕常亮。
// device.keepScreenDim([timeout])  保持屏幕常亮，但允许屏幕变暗来节省电量
// device.cancelKeepingAwake()
// device.vibrate(ms)               使设备震动一段时间。
// device.cancelVibration()         如果设备处于震动状态，则取消震动。










































