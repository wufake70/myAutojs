/**
 * 1.获取 当前运行的脚本的 名字
 * engines.myEngine().getSource()
 * 
 * 2.在脚本里 运行 其他脚本
 *      engines.execScript("hello world", "toast('hello world')");      第一个参数 是 名字，第二个 参数 是 字符串命令
 *      engines.execScriptFile(scriptpath)                              直接运行 一个脚本文件
 * 
 */


